export const mockData = {
  id: 'cid',
  user_id: 'uid',
};

export const mockArray = [mockData];

export const id = mockData.id;
export const uid = mockData.user_id;
export const req = { user: uid };
